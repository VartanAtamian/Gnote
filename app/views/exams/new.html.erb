<div>
  <%= form_for :exam, url: exams_path do |f| %>
    <h3> New exam </h3>
    <div class="form-group">
      <%= f.label :'Exam title' %>
      <%= f.text_field :examTitle, :autofocus => true %>
    </div>
     <%= f.submit 'Submit' %>
  <% end %>
</div>
